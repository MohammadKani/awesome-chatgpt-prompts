{
  "builds": [
    {
      "src": "_config.yml",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "_site"
      }
    }
  ],
  "buildCommand": "gem install jekyll bundler && bundle install && jekyll build"
}
